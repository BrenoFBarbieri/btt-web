import React from 'react';
import styles from './styles.css';

const Header = () => {
	return (
		<header className={styles.header}>
			<nav className={styles.nav}>
				<h1>BTT - Brasil de todos para todos</h1>
			</nav>
		</header>
	)
}

export default Header;
